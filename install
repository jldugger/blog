#!/bin/bash

# generate and install content
pelican ./content/ -s pelicanconf.py

# minify css

for css in `find /var/www/www.pwnguin.net/theme/css/ -name *.css`
do
	cat $css | cssmin | sponge $css
done
